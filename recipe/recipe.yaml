schema_version: 1

context:
  version: 2.0.1

package:
  name: e3sm-quickview
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/e/e3sm-quickview/e3sm_quickview-${{ version }}.tar.gz
  sha256: f7c667a8402ad8f6452a5bf59c29c076e45e8ce60a5320c49f7137fb570f75be

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - quickview = e3sm_quickview.app:main

requirements:
  host:
    - python ${{ python_min }}.*
    - hatchling
    - pip
  run:
    - python >=${{ python_min }}
    - paraview >=6
    - trame >=3.12
    - trame-rca >=2.3.1
    - trame-vuetify >=3.1
    - pyproj >=3.6.1
    - netcdf4 >=1.6.5
    - trame-dataclass >=2.0.1
    - trame-components
    - trame-tauri >=0.6.2
    - pillow

tests:
  - python:
      imports:
        - e3sm_quickview
      pip_check: true
      python_version: ${{ python_min }}.*
  - requirements:
      run:
        - pip
        - python ${{ python_min }}.*
    script:
      - quickview --help

about:
  summary: An application to explore/analyze data for atmosphere component for E3SM
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://pypi.org/project/e3sm-quickview/

extra:
  recipe-maintainers:
    - jourdain
    - psavery
    - alesgenova
